name: Build Android APK

on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Depoyu Kopyala
      uses: actions/checkout@v4

    - name: Node.js Kurulumu
      uses: actions/setup-node@v4
      with:
        node-version: 18

    - name: Projeyi Hazırla ve Logoyu Entegre Et
      run: |
        npm install
        npx cap add android || true
        
        # Eğer icon.png varsa Capacitor assets ile logoyu otomatik uygula
        if [ -f "icon.png" ]; then
          npm install -g @capacitor/assets
          npx @capacitor/assets generate --android || true
        elif [ -f "assets/icon.png" ]; then
          npm install -g @capacitor/assets
          npx @capacitor/assets generate --android || true
        fi
        
        npx cap update android
        npx cap sync android

    - name: İsmi Sabitle
      run: |
        STRING_FILE="android/app/src/main/res/values/strings.xml"
        if [ -f "$STRING_FILE" ]; then
          sed -i 's/<string name="app_name">.*<\/string>/<string name="app_name">Dijital Esnaf Defteri<\/string>/g' "$STRING_FILE"
        fi

    - name: JDK 17 Kurulumu
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: APK Derle ve Yeniden Adlandır
      run: |
        cd android
        chmod +x gradlew
        ./gradlew assembleDebug --no-daemon
        cd ..
        # Çıkan APK dosyasını doğrudan istediğimiz isimle kopyalıyoruz
        cp android/app/build/outputs/apk/debug/app-debug.apk Dijital-Esnaf-Defteri.apk

    - name: APK Kaydet
      uses: actions/upload-artifact@v4
      with:
        name: Dijital-Esnaf-Defteri
        path: Dijital-Esnaf-Defteri.apk
        
